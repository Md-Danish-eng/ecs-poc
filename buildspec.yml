version: 0.2

#env:
  #variables:
      #AWS_Access_Key_ID: "AKIAWRXREH7AKJCNK3NU"
      #AWS_Secret_Access_Key: "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"
  #parameter-store:
     # key: "value"
     # key: "value"
  #secrets-manager:
     # key: secret-id:json-key:version-stage:version-id
     # key: secret-id:json-key:version-stage:version-id
  #exported-variables:
     # - variable
     # - variable
  #git-credential-helper: yes
#batch:
  #fast-fail: true
  #build-list:
  #build-matrix:
  #build-graph:
phases:
  install:
    #If you use the Ubuntu standard image 2.0 or later, you must specify runtime-versions.
    #If you specify runtime-versions and use an image other than Ubuntu standard image 2.0, the build fails.
    #runtime-versions:
      # name: version
      # name: version
    commands:
      - nohup /usr/local/bin/dockerd --host=unix:///var/run/docker.sock --host=tcp://127.0.0.1:2375 --storage-driver=overlay2&
      - timeout 15 sh -c "until docker info; do echo .; sleep 1; done"
pre_build:
    commands:
       #- echo Logging in to Amazon ECR...
       - aws --version
       - aws ecr get-login --region us-east-1 --no-include-email
       - REPOSITORY_URI=450403516352.dkr.ecr.us-east-1.amazonaws.com/danish-bluegreen-poc
       - COMMIT_HASH=$(echo $CODEBUILD_RESOLVED_SOURCE_VERSION | cut -c 1-7)
       - IMAGE_TAG=build-$(echo $CODEBUILD_BUILD_ID | awk -F":" '{print $2}')
build:
    commands:
       - docker build -t $REPOSITORY_URI:latest  .
       - docker tag $REPOSITORY_URI:latest $REPOSITORY_URI:$IMAGE_TAG
post_build:
    commands:
       - docker push $REPOSITORY_URI:latest
       - docker push $REPOSITORY_URI:$IMAGE_TAG
       - printf '[{"name":"web","imageUri":"%s"}]' $REPO_URL:$IMAGE_TAG > imagedefinitions.json
       
       #- sed -i 's/Tmt-prod-web:latest/Tmt-prod-web:$IMAGE_TAG/g' appspec.yaml
       #- sed -i 's/Tmt-prod-web:latest/Tmt-prod-web:$IMAGE_TAG/g' AppSpec.yaml
       
       
       
#reports:
  #report-name-or-arn:
    #files:
      # - imagedefinitions.json
      # - location
    #base-directory: location
    #discard-paths: yes
    #file-format: JunitXml | CucumberJson
artifacts:
  files:
    - appspec.yaml
    - imagedefinitions.json
  
    
    # - location
    # - location
  #name: $(date +%Y-%m-%d)
  #discard-paths: yes
  #base-directory: location
#cache:
  #paths:
    # - paths
